# Secrets — never commit live credentials
.env
admin/.env
--settingsEnv/.env

# Customer-uploaded order receipts — contain personal payment details
assets/img/receipts/

# Local build / deploy artifacts
admin/node_modules/
*.zip
kltg_stage/
